# Design

Visual system for the site: type scale, spacing, card layout, code block styling.

Colors and tokens live in `src/styles/tokens.css`. Read this file when a task
touches visual styling; for a single slide fix, read only the relevant section.
