# Architecture

Static site: Astro collects Markdown under `content/`, builds to `dist/`.
Deck pages render from a single layout component. No client JS except the
small caption toggle.

Read this file when changing build tooling or the layout pipeline.
